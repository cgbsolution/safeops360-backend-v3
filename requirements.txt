fpdf2
# EHS Scorecard deck export. Pure-Python (lxml + Pillow only), no system
# binaries — the airgapped deployment constraint rules out LibreOffice headless.
python-pptx>=1.0.0
openpyxl>=3.1.0

# zoneinfo has no tz database on Windows (and on slim Linux images without
# tzdata), so report timestamps silently fell back to UTC while labelled IST.
tzdata>=2024.1
segno==1.6.6
